defmodule FileSize.ParserTest do
  use ExUnit.Case

  alias FileSize.Parser

  describe "parse/1" do
    test "success with Bit struct"

    test "success with Byte struct"

    test "success with string"

    test "error"
  end

  describe "parse/2" do
    test "success with Bit struct"

    test "success with Byte struct"

    test "success with string"

    test "error"
  end
end
